# GitLab Configuration for omniscript-gitlab-automation-agent
# Copy this file to .env and fill in your actual values

# SSH Key Setup (for git clone/push operations)
# Run these commands to set up SSH authentication:
#   ssh-keygen -t ed25519 -C "your.email@company.com" -f ~/.ssh/gitlab_ed25519
#   ssh-add ~/.ssh/gitlab_ed25519
#   cat ~/.ssh/gitlab_ed25519.pub | pbcopy
# Then add the public key to GitLab → Preferences → SSH Keys
# Test: ssh -T git@gitlab.com

# GitLab Personal Access Token for API operations (merge request creation)
# Create at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: api, read_repository, write_repository
GITLAB_DOCS_TOKEN=your-docs-repo-personal-access-token-here

# GitLab Username (optional - will use git config if not set)
GITLAB_USERNAME=your.username

# GitLab Email (optional - will use git config if not set)
GITLAB_EMAIL=your.email@company.com

# GitLab Host (optional - defaults to gitlab.com)
# For self-hosted GitLab: gitlab.yourcompany.com
GITLAB_HOST=gitlab.com
