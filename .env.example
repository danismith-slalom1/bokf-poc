# GitLab Configuration for omniscript-gitlab-automation-agent
# Copy this file to .env and fill in your actual values

# GitLab Personal Access Token
# Create at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: api, read_repository, write_repository
GITLAB_TOKEN=your-gitlab-personal-access-token-here

# GitLab Username (optional - will use git config if not set)
GITLAB_USERNAME=your.username

# GitLab Email (optional - will use git config if not set)
GITLAB_EMAIL=your.email@company.com

# GitLab Host (optional - defaults to gitlab.com)
# For self-hosted GitLab: gitlab.yourcompany.com
GITLAB_HOST=gitlab.com
